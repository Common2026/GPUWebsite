// src/index.js
import express from 'express';

const app = express();
const PORT = process.env.PORT || 3000;

// Middleware (optional, add JSON parsing if needed)
app.use(express.json());

// Health check route
app.get('/health', (req, res) => {
  res.status(200).send('OK');
});

// Root route
app.get('/', (req, res) => {
  res.send('GPU Resale Portal is running');
});

// Example API route
app.get('/api/status', (req, res) => {
  res.json({
    service: 'GPU Resale Portal',
    status: 'online',
    timestamp: new Date().toISOString()
  });
});

// Catch-all for undefined routes
app.use((req, res) => {
  res.status(404).send('Not Found');
});

// Start server
app.listen(PORT, () => {
  console.log(`Server listening on port ${PORT}`);
});
// src/index.js
import express from 'express';

const app = express();
const PORT = process.env.PORT || 3000;

// Middleware (optional, add JSON parsing if needed)
app.use(express.json());

// Health check route
app.get('/health', (req, res) => {
  res.status(200).send('OK');
});

// Root route
app.get('/', (req, res) => {
  res.send('GPU Resale Portal is running');
});

// Example API route
app.get('/api/status', (req, res) => {
  res.json({
    service: 'GPU Resale Portal',
    status: 'online',
    timestamp: new Date().toISOString()
  });
});

// Catch-all for undefined routes
app.use((req, res) => {
  res.status(404).send('Not Found');
});

// Start server
app.listen(PORT, () => {
  console.log(`Server listening on port ${PORT}`);
});

